<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.green.board3.UserMapper">
    <insert id="insBoard" parameterType="UserEntity">
    INSERT INTO t_user
    ( uid, upw, nm, gender, addr )
    VALUES
    (#{uid},#{upw},#{nm},#{gender},#{addr})
    </insert>

</mapper>